{
    "version": 2,
    "functions": {
        "api/**/*.js": {
            "runtime": "@vercel/node@latest"
        }
    },
    "routes": [
        { "src": "/api/generate", "dest": "/api/generate" },
        { "src": "/(.*)", "dest": "/$1" }
    ]
}
